import {render,screen} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {beforeEach,describe,expect,it,vi} from "vitest";import {Dashboard} from "../Dashboard";
describe("Front Desk checklist navigation",()=>{beforeEach(()=>{localStorage.clear();vi.stubGlobal("scrollTo",vi.fn());vi.spyOn(HTMLCanvasElement.prototype,"getContext").mockReturnValue({scale:vi.fn(),fillRect:vi.fn(),beginPath:vi.fn(),moveTo:vi.fn(),lineTo:vi.fn(),stroke:vi.fn()} as unknown as CanvasRenderingContext2D)});it("opens checklists, returns home, and hides access after a role change",async()=>{const user=userEvent.setup();render(<Dashboard/>);expect(screen.getByText("Today’s Front Desk Checklists")).toBeVisible();await user.click(screen.getByRole("button",{name:"Open Checklists"}));expect(screen.getByRole("heading",{name:"Front Desk Checklist"})).toBeVisible();await user.click(screen.getByRole("button",{name:"Go to Dashboard"}));expect(screen.getByRole("heading",{name:"Front Desk Operations"})).toBeVisible();await user.click(screen.getByRole("button",{name:"Open Checklists"}));await user.selectOptions(screen.getByLabelText("Demo role"),"HOUSEKEEPING_SUPERVISOR");expect(screen.getByRole("heading",{name:"Housekeeping Operations"})).toBeVisible();await user.click(screen.getByRole("button",{name:/Operations/}));expect(screen.queryByRole("menuitem",{name:/Front Desk Checklist/})).not.toBeInTheDocument()})});
