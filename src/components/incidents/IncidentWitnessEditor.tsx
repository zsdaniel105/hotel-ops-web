"use client";
import {useState,type FormEvent} from "react";import {Modal} from "@/components/dashboard/ui/Modal";import type {IncidentWitness} from "@/types/incidents";
export function IncidentWitnessEditor({witnesses,onAdd,onDelete}:{witnesses:IncidentWitness[];onAdd:(w:Omit<IncidentWitness,"id">)=>void;onDelete:(id:string)=>void}){const[open,setOpen]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),name=String(f.get("name")??"").trim();if(!name)return;onAdd({name,contactInformation:String(f.get("contact")??"").trim()||null,statement:String(f.get("statement")??"").trim()||null});setOpen(false)}return <section className="rounded-xl border p-3"><div className="flex justify-between"><h3 className="font-bold">Witnesses</h3><button className="btn-secondary" onClick={()=>setOpen(true)}>Add Witness</button></div>{witnesses.map(w=><div key={w.id} className="mt-2 flex justify-between text-sm"><span>{w.name}{w.statement?` — ${w.statement}`:""}</span><button className="text-red-700" onClick={()=>onDelete(w.id)}>Delete</button></div>)}{!witnesses.length?<p className="text-sm text-slate-500">None recorded.</p>:null}{open?<Modal title="Add witness" onClose={()=>setOpen(false)}><form onSubmit={submit} className="space-y-3"><label className="field">Name<input name="name" required className="form-input"/></label><label className="field">Contact Information<input name="contact" className="form-input"/></label><label className="field">Statement<textarea name="statement" className="form-input"/></label><div className="flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="btn-primary">Add Witness</button></div></form></Modal>:null}</section>}
